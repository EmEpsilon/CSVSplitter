# テストパターン一覧

本ファイルは `CSVSplitter.Tests` で実装したテスト項目の一覧です。  
すべてのテストで、入力 CSV はテストコード内で一時ディレクトリへ自動生成しています。

## 1. ソート処理
- **CSVの基本ソート**
  - 数値列 (`Id`) を昇順でソートし、並び順が期待値どおりになること。
- **ソート条件の組み合わせ**
  - 文字列昇順・数値降順を複合指定した場合に想定順序となること。

## 2. 分割処理
- **最大行数超過時の分割**
  - `MaxCsvRecords` を小さい値に設定し、同一分割キーのデータが複数ファイルに分割されること。
  - このアプリ仕様（ヘッダー行も行数カウント対象）に基づき、分割結果を検証すること。
  - 各出力ファイルのレコード数が上限以内であること。

## 3. 複数ファイル統合 + ソート + 分割
- 複数 CSV を統合後、ソートしてから分割した結果が正しいこと。
- 同一キーグループが行数上限で複数ファイルへ分割されること。

## 4. ソート時の最大レコード数超過ケース
- `GetMaxSortFileRecords` で得られる閾値を超える行数の CSV を生成。
- 内部の一時分割ソート + マージ経路で正しく昇順になること。

## 5. 文字コード判定
以下を網羅し、`InputFile.Analyze()` で CSV として認識されることを検証:
- UTF-8 (BOMあり / なし)
- UTF-16 LE (BOMあり / なし)
- UTF-16 BE (BOMあり / なし)
- Shift-JIS
- EUC-JP
- 日本語文字あり / ASCII 混在
- ひらがな / 全角カタカナ / 半角カナ
- 小さいファイル
- 十分大きいファイル

## 6. 追加した補助的検証
- 出力ファイル命名・分割結果の妥当性（期待ファイル数・各ファイルの行数）
- 解析後の基本属性（区切り文字、改行コード、ヘッダー取得）が妥当であること
- 改行コード（LF）とヘッダー文字列が出力時に維持されること
- ダブルクォートを含む CSV フィールドがソート処理で壊れないこと
- 複数分割キー（例: Group + Type）でファイル分割されること
- 出力ファイル名の無効文字・空白の正規化と連番付与が正しいこと

## 7. 異常系・判定境界
- 入力ファイル未存在時に解析不能として扱われること
- CSV 形式でないテキストを CSV 扱いしないこと
- バイナリ混在データを CSV 扱いしないこと
- `InputFiles.HasUniformHeaders` がヘッダー/文字コード不一致で `false` になること
- `ConvertCommand.CanExecute` が前提条件（入力有無・解析済み・処理中フラグ）で切り替わること

## 8. 統合/性質ベースに近い追加検証
- ソート済み複数ファイルのマージ結果が単調増加順となること
- ランダム生成データを対象に、複合ソート後の単調順序性を検証すること
